language: python
python:
  - "3.5"
# command to install dependencies
# install: "pip install -r requirements.txt"
# command to run tests
script: python ./test.py

notifications:
  email:
    recipients:
      - i.stepykin@gmail.com
    on_success: never  # default: change
    on_failure: always # default: always